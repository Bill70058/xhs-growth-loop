# NEXT STEPS

> 当前主目标切换为：`docs/MVP_TODO.md`（按优先级 1->6 执行）

## 优化方向（2026-03-09）
1. 稳定性优先：降低登录态失效、页面选择器变更、空数据导致的链路中断。
2. 可观测性补齐：把采集/生成/发布关键指标统一落库并做失败告警。
3. 策略质量升级：从“模板生成”升级到“特征学习 + A/B 自动决策”。

## P0（今天就做）
1. 用前端跑一轮“意图 -> 选题 -> 草稿 -> 预览”闭环：
   - 启动 `frontend` 与 `bridge`
   - 在右上角确认账号，必要时新增并登录
   - 点击候选贴文后触发“进入预览发布”
2. 验证 `publish_records` 入库：
   - 至少确认 1 条 `preview` 记录状态为 `READY_TO_PUBLISH` 或 `FAILED`
3. 把本次运行结果写入 `CURRENT_STATUS.md`（账号、候选、预览状态、异常点）。
4. 启动 `OpenClaw Autopilot` 保持持续推进，并观察 `docs/LATEST_PROGRESS.md` 评分变化。

## P1（本周完成）
1. 前端“一键触发市场采集”已完成，下一步增加“采集完成后自动刷新分析与词云”。
2. 增加账号状态巡检（右上角显示当前账号 creator/home 登录状态）。
3. 增加失败重试与告警（采集失败、发布失败、空数据）。
4. 已完成 Phase 1 核心：实验落库 + 回流同步 + Thompson Sampling 基础选臂。
5. 已完成 Runtime Bridge 接入，下一步将 runtime 决策从 mock 迁移到真实 OpenClaw。
6. 为 Autopilot 增加“达到 MVP 阈值后自动降频”策略。

## P2（两周内）
1. 完成 Phase 2 深化：奖励归因从标题匹配升级到 note_id 级别。
2. 增加每周复盘报告（自动输出 top/bottom 内容特征）。
3. 接入真实 OpenClaw 自动任务（日报、候选生成、周复盘），替换本地 mock。
4. 将前端本地规则解析替换为 OpenClaw 在线解析并沉淀提示模板。
5. 增加对 `legacy_pending_publish_records` 的迁移修复脚本（历史数据补 run_id 关联）。
6. 将 runtime 决策记录纳入前端看板（失败类型、重试次数、最终处置）。

## 完成判定
- 每天至少稳定输出：
  - 1 份非空数据摘要（或明确空数据原因）
  - 3 条候选贴文
  - 1 次预览发布
- 每周能看到策略更新痕迹，而不是重复同一模板。
